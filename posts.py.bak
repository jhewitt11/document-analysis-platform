import requests
import os

#print("\ncurrent directory is : ", os.listdir(), "\n")

resp = requests.post("http://localhost:5000/summarize",
                     files={"file": open('input.txt')})
                     

print("Response is : \n", resp.json())